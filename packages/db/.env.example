# Prisma datasource. Copy to `.env`. Matches docker-compose postgres defaults.
DATABASE_URL="postgresql://perps:perps@localhost:5432/perps"

# Demo market id seeded by `prisma/seed.ts`. Must match the value used by the
# engine and liquidation engine.
MARKET_ID=cmqgd206q00002bxy0wavuaff
